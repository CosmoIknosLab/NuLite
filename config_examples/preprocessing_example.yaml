# dataset paths
wsi_paths: /work/temp.svs
output_path: /tmp/nulite_inference/

# basic setups
patch_size: 1024
patch_overlap: 6.25
target_mag: 40
processes: 1
overwrite: True

# macenko stain normalization
normalize_stains: False

# finding patches
min_intersection_ratio: 0.05
